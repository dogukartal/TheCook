---
phase: 02-profile-and-auth
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - TheCook/package.json
  - TheCook/__tests__/profile.test.ts
  - TheCook/__tests__/migration.test.ts
  - TheCook/__tests__/sync.test.ts
autonomous: true
requirements:
  - ONBRD-01
  - ONBRD-02
  - ONBRD-03
  - AUTH-01
  - AUTH-02
  - AUTH-03

must_haves:
  truths:
    - "All new Phase 2 dependencies are installed without peer dep conflicts"
    - "Test stubs exist for profile schema, DB migration, and sync — all failing with clear 'not yet implemented' messages"
    - "jest runs cleanly (passWithNoTests) after stubs are added alongside existing Phase 1 tests"
  artifacts:
    - path: "TheCook/__tests__/profile.test.ts"
      provides: "Test stubs for ONBRD-01, ONBRD-02, ONBRD-03 (ProfileSchema validation, allergen opt-in)"
    - path: "TheCook/__tests__/migration.test.ts"
      provides: "Test stubs for AUTH-01 (DB_VERSION 2 creates profile + bookmarks tables)"
    - path: "TheCook/__tests__/sync.test.ts"
      provides: "Test stubs for AUTH-02, AUTH-03 (cloud-wins upsert, sign-out preserves local data)"
  key_links:
    - from: "TheCook/__tests__/profile.test.ts"
      to: "TheCook/src/types/profile.ts"
      via: "import ProfileSchema"
      pattern: "from.*types/profile"
    - from: "TheCook/__tests__/migration.test.ts"
      to: "TheCook/src/db/client.ts"
      via: "import migrateDb"
      pattern: "from.*db/client"
---

<objective>
Install Phase 2 dependencies and create failing test stubs that will guide TDD implementation in subsequent plans.

Purpose: The Nyquist rule requires every task have an automated verify. This plan creates the test files first so Plans 02-04 can reference them as verify targets. Stubs fail with clear intent — not undefined-crashes.
Output: Installed deps, three test files with failing stubs covering all 6 Phase 2 requirements.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profile-and-auth/02-CONTEXT.md
@.planning/phases/02-profile-and-auth/02-RESEARCH.md
@.planning/phases/02-profile-and-auth/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Phase 2 dependencies</name>
  <files>TheCook/package.json</files>
  <action>
    Run from the TheCook/TheCook/ directory (where package.json lives):

    ```
    npx expo install @supabase/supabase-js expo-apple-authentication expo-crypto react-native-url-polyfill
    npx expo install @react-native-google-signin/google-signin
    ```

    After installation, verify all 5 packages appear in dependencies in package.json. Do NOT run `npm install` or `yarn add` — use `expo install` to get SDK-compatible versions.

    Also create TheCook/TheCook/.env.local with placeholder values (file will be populated in Plan 02-02):
    ```
    EXPO_PUBLIC_SUPABASE_URL=
    EXPO_PUBLIC_SUPABASE_ANON_KEY=
    EXPO_PUBLIC_GOOGLE_WEB_CLIENT_ID=
    EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
    ```

    Add .env.local to .gitignore if not already present.
  </action>
  <verify>
    <automated>cd TheCook/TheCook && npx jest --passWithNoTests 2>&1 | tail -5</automated>
  </verify>
  <done>package.json contains @supabase/supabase-js, expo-apple-authentication, expo-crypto, react-native-url-polyfill, @react-native-google-signin/google-signin; existing 21 tests still pass; .env.local exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create failing test stubs</name>
  <files>
    TheCook/__tests__/profile.test.ts
    TheCook/__tests__/migration.test.ts
    TheCook/__tests__/sync.test.ts
  </files>
  <behavior>
    profile.test.ts stubs:
    - ONBRD-01: ProfileSchema rejects invalid allergen value (e.g., "pollen") — fails until ProfileSchema exists
    - ONBRD-01: allergens array defaults to [] — none pre-selected (safety-critical: opt-in)
    - ONBRD-02: SkillLevelEnum values are exactly "beginner" | "intermediate" | "advanced"
    - ONBRD-02: skillLevel defaults to null
    - ONBRD-03: equipment defaults to ["fırın", "tava"] (oven + stovetop pre-selected)
    - ONBRD-03: EquipmentEnum values from recipe.ts are reused (no duplication)

    migration.test.ts stubs:
    - AUTH-01: after migrateDb runs against DB_VERSION 2, profile table exists
    - AUTH-01: after migrateDb, bookmarks table exists with id, recipe_id, user_id, created_at columns
    - AUTH-01: profile row with id=1 is seeded with onboarding_completed=0 by default

    sync.test.ts stubs:
    - AUTH-02: pullCloudProfile() with mock cloud data overwrites local profile (cloud wins)
    - AUTH-03: after sign-out, local profile data is still accessible in SQLite
  </behavior>
  <action>
    Create each test file with real describe/it blocks that will fail with "Cannot find module" or "X is not defined" until the implementation files exist. Do NOT write passing tests yet — the stubs must fail. Use `todo` tests (jest.todo) or `expect(true).toBe(false)` with a comment like `// TODO: implement in Plan 02-03` only as a last resort — prefer real import + call that will fail naturally.

    Pattern from existing tests (see TheCook/__tests__/schema.test.ts for reference):
    - Import the module to be created
    - Write assertions that describe the expected behavior
    - The test fails because the module doesn't exist yet — this is correct

    For sync.test.ts: mock the Supabase client inline using jest.mock('../../src/auth/supabase') so the test doesn't require a live Supabase connection.

    For migration.test.ts: use expo-sqlite's in-memory database. Import migrateDb from src/db/client — it already exists from Phase 1. The test will confirm that after bump to DB_VERSION 2, the new tables exist.

    Keep stubs minimal — one clear assertion per behavior. Real implementation happens in later plans.
  </action>
  <verify>
    <automated>cd TheCook/TheCook && npx jest __tests__/profile.test.ts __tests__/migration.test.ts __tests__/sync.test.ts 2>&1 | grep -E "(FAIL|PASS|Cannot find|not defined|Tests:)" | head -20</automated>
  </verify>
  <done>Three test files exist; tests fail due to missing modules (not due to syntax errors or import misconfigurations); `npx jest --passWithNoTests` still passes for the full suite (because new failing tests are expected-failing stubs)</done>
</task>

</tasks>

<verification>
Run full suite after both tasks complete:
```
cd TheCook/TheCook && npx jest --passWithNoTests
```
Existing 21 Phase 1 tests must still pass. New stubs may fail — that is expected and correct.
</verification>

<success_criteria>
- 5 new packages installed and in package.json
- .env.local exists with placeholder keys
- 3 test stub files exist covering all 6 requirement IDs
- Full jest suite runs without crashing (--passWithNoTests)
- Stub failures are "module not found" type, not syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-and-auth/02-01-SUMMARY.md`
</output>
