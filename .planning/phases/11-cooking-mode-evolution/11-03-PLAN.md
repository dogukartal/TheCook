---
phase: 11-cooking-mode-evolution
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - TheCook/content/recipes/menemen.yaml
  - TheCook/content/recipes/mercimek-corbasi.yaml
  - TheCook/content/recipes/borek.yaml
  - TheCook/assets/data/recipes.json
autonomous: false
requirements: [COOKX-01, COOKX-02, COOKX-03]

must_haves:
  truths:
    - "3 sample recipes have checkpoint and warning content on at least 2 steps each"
    - "recipes.json is rebuilt with checkpoint and warning fields present"
    - "Full test suite passes after recipe content update"
    - "Cooking mode visually displays step image fallback, checkpoint, and warning on device"
    - "Completing a recipe shows star rating and logs to cooking_history"
    - "Exit during cooking triggers confirmation dialog"
  artifacts:
    - path: "TheCook/content/recipes/menemen.yaml"
      provides: "Sample recipe with checkpoint and warning fields"
      contains: "checkpoint:"
    - path: "TheCook/content/recipes/mercimek-corbasi.yaml"
      provides: "Sample recipe with checkpoint and warning fields"
      contains: "checkpoint:"
    - path: "TheCook/content/recipes/borek.yaml"
      provides: "Sample recipe with checkpoint and warning fields"
      contains: "checkpoint:"
    - path: "TheCook/assets/data/recipes.json"
      provides: "Rebuilt recipe bundle with new fields"
      contains: "checkpoint"
  key_links:
    - from: "TheCook/content/recipes/*.yaml"
      to: "TheCook/assets/data/recipes.json"
      via: "npm run build-recipes"
      pattern: "build-recipes"
---

<objective>
Update 3 sample recipes with checkpoint and warning content, rebuild recipes.json, and verify all Phase 11 features end-to-end on device.

Purpose: Schema and UI changes from Plans 01-02 need recipe content to demonstrate. Human verification confirms visual polish and completion flow work correctly.
Output: 3 updated recipe YAMLs, rebuilt recipes.json, verified cooking mode.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cooking-mode-evolution/11-RESEARCH.md
@.planning/phases/11-cooking-mode-evolution/11-01-SUMMARY.md
@.planning/phases/11-cooking-mode-evolution/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add checkpoint and warning content to 3 sample recipes and rebuild</name>
  <files>TheCook/content/recipes/menemen.yaml, TheCook/content/recipes/mercimek-corbasi.yaml, TheCook/content/recipes/borek.yaml, TheCook/assets/data/recipes.json</files>
  <action>
    1. Edit 3 recipe YAML files to add `checkpoint` and `warning` fields to their steps. Add to at least 2 steps per recipe. Leave remaining steps with `checkpoint: null` and `warning: null`. Content should be realistic Turkish cooking guidance (short one-liners, max ~60 chars each).

    For menemen.yaml example step additions:
    - Step "Sebzeleri dograyin": checkpoint: "Domates kupler 1-2 cm boyutunda" / warning: "Cok buyuk dograma pisirme suresini uzatir"
    - Step "Sogan ve biberi kavurun": checkpoint: "Soganlar seffaf, biberler parlak yesil" / warning: "Ates cok yuksekse tereyagi yanar"
    - Step "Domatesi pisirin": checkpoint: "Sos kasik izini tutacak kivamda" / warning: null
    - Step "Yumurtalari kirin": checkpoint: "Yumurta beyazlari donmus, sarilari akiskan" / warning: "Cok karistirma — yumurtalar parcalanir"

    For mercimek-corbasi.yaml — similar pattern: add checkpoint/warning to 2-3 cooking-critical steps.
    For borek.yaml — similar pattern: add checkpoint/warning to 2-3 key steps.

    2. Run `cd TheCook && npm run build-recipes` to regenerate `TheCook/assets/data/recipes.json` from YAML sources.

    3. Run `cd TheCook && npx jest --no-coverage` to verify full test suite passes with updated recipe data.

    NOTE: The remaining 27 recipes do NOT need updating — StepSchema defaults checkpoint and warning to null, so they parse cleanly without these fields.
  </action>
  <verify>
    <automated>cd TheCook && npm run build-recipes && npx jest --no-coverage</automated>
  </verify>
  <done>3 sample recipes contain checkpoint and warning fields. recipes.json is rebuilt. Full test suite passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Phase 11 cooking mode evolution on device</name>
  <action>
    Human verification of all Phase 11 features:
    - COOKX-01: Step images with fallback color blocks
    - COOKX-02: Checkpoint (green callout) and Warning (amber callout) per step
    - COOKX-03: Star rating on completion screen, cooking history logging, exit confirmation modal
  </action>
  <verify>
    Steps to verify:
    1. Start the dev server: `npx expo start`
    2. Open any of the 3 updated recipes (Menemen, Mercimek Corbasi, or Borek)
    3. Enter cooking mode (tap "Pisirmeye Basla")
    4. Verify on a step with checkpoint/warning content:
       - Green "check-circle" callout appears with checkpoint text
       - Amber "alert" callout appears with warning text
       - Existing red Dikkat section still shows commonMistake + recovery below
    5. Swipe to a step WITHOUT checkpoint/warning — verify no callout sections appear
    6. Verify step image area shows pastel color block (expected — no images authored yet)
    7. Press the close (X) button — verify exit confirmation Alert appears with "Devam et" and "Cik" options
    8. Press "Devam et" — verify cooking continues
    9. Navigate to the last step and tap "Bitir"
    10. Verify celebration screen shows:
        - "Afiyet olsun!" title, recipe name, cooking time
        - 5 star rating icons (all outline initially)
    11. Tap 4th star — verify stars 1-4 fill gold
    12. Tap "Tariflere Don" — verify app navigates back to recipe list
    13. Go to Feed — verify "Denemediklerin" section no longer shows the completed recipe (logged to cooking_history)
  </verify>
  <done>All Phase 11 features verified on device. Checkpoint/warning callouts render correctly. Star rating works. Completion logs to history. Exit confirmation modal functional.</done>
</task>

</tasks>

<verification>
cd TheCook && npx jest --no-coverage
</verification>

<success_criteria>
- 3 sample recipes have checkpoint/warning content in YAML
- recipes.json rebuilt with new fields
- Cooking mode shows checkpoint (green) and warning (amber) callouts
- Step image area shows pastel fallback (no images yet)
- Completion screen has star rating widget
- Completion logs to cooking_history
- Exit confirmation modal works
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/11-cooking-mode-evolution/11-03-SUMMARY.md`
</output>
