---
phase: 14-color-token-sweep
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - TheCook/components/ui/recipe-card-grid.tsx
  - TheCook/components/ui/recipe-card-row.tsx
  - TheCook/components/ui/skeleton-card.tsx
  - TheCook/components/ui/feed-section.tsx
  - TheCook/app/recipe/[id].tsx
  - TheCook/app/(tabs)/index.tsx
  - TheCook/app/(tabs)/search.tsx
  - TheCook/app/(tabs)/profile.tsx
  - TheCook/app/(tabs)/cookbook.tsx
  - TheCook/app/(tabs)/_layout.tsx
  - TheCook/app/settings.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Recipe cards use colors.card background and colors.cardBorder border in both grid and row layouts"
    - "Dark mode cards have visible boundary via cardBorder token (borderWidth: 1 in dark mode)"
    - "Recipe detail screen uses theme tokens for all non-gradient colors"
    - "All isDark ternaries that map to existing tokens are replaced with token references"
    - "CATEGORY_GRADIENTS imported from palette.ts instead of duplicated inline"
    - "Settings screen uses theme tokens for all backgrounds, text, and borders"
    - "Profile screen eliminates isDark ternaries where tokens exist"
  artifacts:
    - path: "TheCook/components/ui/recipe-card-grid.tsx"
      provides: "Theme-tokenized grid card with UX-01 dark border"
      contains: "colors.cardBorder"
    - path: "TheCook/components/ui/recipe-card-row.tsx"
      provides: "Theme-tokenized row card with UX-01 dark border"
      contains: "colors.cardBorder"
    - path: "TheCook/app/recipe/[id].tsx"
      provides: "Theme-tokenized recipe detail"
      contains: "CATEGORY_GRADIENTS"
    - path: "TheCook/app/settings.tsx"
      provides: "Theme-tokenized settings screen"
      contains: "colors.surface"
  key_links:
    - from: "TheCook/components/ui/recipe-card-grid.tsx"
      to: "TheCook/constants/palette.ts"
      via: "import CATEGORY_GRADIENTS"
      pattern: "import.*palette"
    - from: "TheCook/components/ui/recipe-card-row.tsx"
      to: "TheCook/constants/palette.ts"
      via: "import CATEGORY_GRADIENTS"
      pattern: "import.*palette"
    - from: "TheCook/app/recipe/[id].tsx"
      to: "TheCook/constants/palette.ts"
      via: "import CATEGORY_GRADIENTS, STEP_PASTEL_BACKGROUNDS"
      pattern: "import.*palette"
    - from: "TheCook/components/ui/recipe-card-grid.tsx"
      to: "TheCook/constants/theme.ts"
      via: "useAppTheme() colors"
      pattern: "colors\\.cardBorder"
---

<objective>
Sweep all hardcoded hex/rgba values from recipe card components (UX-01 core) and high-visibility core screens, replacing them with theme tokens from the expanded theme.ts and palette imports from palette.ts.

Purpose: Recipe cards are the primary UX-01 deliverable — they must have visible dark mode contrast. Core screens (feed, search, profile, cookbook, settings, recipe detail) are the highest-traffic surfaces users interact with. This plan directly satisfies UX-01 for the most visible UI.

Output: 11 files swept clean of hardcoded colors, cards with proper dark mode borders.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-color-token-sweep/14-RESEARCH.md
@.planning/phases/14-color-token-sweep/14-01-SUMMARY.md

<interfaces>
<!-- New tokens available after Plan 01 (in addition to existing 11) -->
From TheCook/constants/theme.ts (expanded):
```typescript
// New tokens available in colors.X:
tintBg, onTint, textSecondary, cardBorder, shadow,
success, successBg, warning, warningBg, error, errorBg,
disabledIcon, overlay, skeleton, placeholder, inputBg, separator
```

From TheCook/constants/palette.ts:
```typescript
export const CATEGORY_GRADIENTS: Record<string, [string, string]>;
export const DEFAULT_GRADIENT: [string, string];
export const STEP_PASTEL_BACKGROUNDS: string[];
export const CATEGORY_STRIP_COLORS: Record<string, string>;
export const STAR_RATING_COLOR: string;
export const GOOGLE_BRAND_BLUE: string;
```

<!-- useAppTheme hook pattern (used by all components) -->
From TheCook/contexts/ThemeContext.tsx:
```typescript
export function useAppTheme(): { isDark: boolean; colors: typeof Colors.light; toggleTheme: () => void };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep recipe card components (UX-01 core)</name>
  <files>TheCook/components/ui/recipe-card-grid.tsx, TheCook/components/ui/recipe-card-row.tsx, TheCook/components/ui/skeleton-card.tsx, TheCook/components/ui/feed-section.tsx</files>
  <action>
Read each file first, then apply these changes:

**recipe-card-grid.tsx** (16 hex values):
1. Remove the inline `CATEGORY_GRADIENTS` constant. Add `import { CATEGORY_GRADIENTS, DEFAULT_GRADIENT } from '@/constants/palette';` instead.
2. Replace hardcoded `#FFFFFF` card background in StyleSheet with inline `{ backgroundColor: colors.card }`. Keep non-color properties (borderRadius, padding, overflow, shadow offsets) in StyleSheet.
3. Add UX-01 dark mode card boundary: `borderColor: colors.cardBorder` and `borderWidth: isDark ? 1 : 0` in the card style array.
4. Replace `shadowColor: '#000000'` with inline `{ shadowColor: colors.shadow }`.
5. Replace hardcoded badge/label colors:
   - Badge background (like `#FEF3EC` or similar light tint bg) -> `colors.tintBg`
   - Badge text (terracotta/brand) -> `colors.tint`
   - Cook time text -> `colors.textSub`
   - Title text -> `colors.text`
   - Any `isDark ? X : Y` ternaries that map to existing tokens -> the token directly
6. Move all theme-dependent colors from StyleSheet.create to inline style arrays. Non-color styles (borderRadius, padding, flex, margin, fontSize) stay in StyleSheet.

**recipe-card-row.tsx** (16 hex values):
Apply same pattern as recipe-card-grid.tsx:
1. Replace inline `CATEGORY_GRADIENTS` with palette.ts import
2. Card background -> `colors.card`, add `cardBorder` + conditional borderWidth for dark mode (UX-01)
3. Shadow -> `colors.shadow`
4. Badge/text colors -> appropriate theme tokens
5. Remove all isDark ternaries that map to tokens

**skeleton-card.tsx** (6 hex values):
1. Replace hardcoded skeleton/shimmer colors with `colors.skeleton` and `colors.card` tokens
2. Move color values from StyleSheet to inline style arrays

**feed-section.tsx** (1 hex value):
1. Replace hardcoded section title color with `colors.text` or `colors.textSecondary` as appropriate

For ALL files:
- Keep `const { isDark, colors } = useAppTheme();` at top of component (already exists in most)
- Add `useAppTheme` import if missing
- Do NOT change non-color styles (borderRadius, padding, margins, fontSize, fontWeight, etc.)
- Do NOT theme-switch gradient colors — they come from palette.ts and stay as-is
- The equipment badge amber color (`#D97706`) should become `colors.warning`
- For colors used on top of gradient backgrounds (like white text over gradient), use `colors.onTint` or keep as `'#FFFFFF'` since it's white regardless of theme (add `// on-gradient` comment)
  </action>
  <verify>
    <automated>cd /Users/sado/Documents/Projects/TheCook/TheCook && npx tsc --noEmit 2>&1 | head -20 && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 card/UI files use theme tokens exclusively for non-decorative colors. CATEGORY_GRADIENTS imported from palette.ts (not duplicated). Recipe cards have colors.cardBorder + conditional borderWidth for dark mode (UX-01). Zero hardcoded hex values remain in these files except gradient palette references from palette.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep core screens (recipe detail, tabs, settings)</name>
  <files>TheCook/app/recipe/[id].tsx, TheCook/app/(tabs)/index.tsx, TheCook/app/(tabs)/search.tsx, TheCook/app/(tabs)/profile.tsx, TheCook/app/(tabs)/cookbook.tsx, TheCook/app/(tabs)/_layout.tsx, TheCook/app/settings.tsx</files>
  <action>
Read each file first, then apply changes. For each file, the pattern is the same:
1. Replace inline gradient/pastel palettes with palette.ts imports
2. Replace hardcoded hex/rgba colors with theme tokens
3. Replace isDark ternaries with tokens where the values match
4. Move color-dependent styles from StyleSheet.create to inline style arrays

**recipe/[id].tsx** (29 hex values — largest file):
1. Remove inline `CATEGORY_GRADIENTS` and `STEP_PASTEL_BACKGROUNDS`. Import from `@/constants/palette`.
2. Replace background colors with `colors.background`, `colors.surface`, `colors.card`
3. Replace text colors with `colors.text`, `colors.textSub`, `colors.textSecondary`, `colors.textMuted`
4. Replace section dividers/borders with `colors.border`, `colors.separator`
5. Replace badge backgrounds with `colors.tintBg`, badge text with `colors.tint`
6. Replace isDark ternaries: `isDark ? '#161614' : '#F0EDE8'` -> `colors.card`, `isDark ? '#0C0C0A' : '#FFFFFF'` -> `colors.background`, etc.
7. White text on gradient overlays -> `colors.onTint` or `'#FFFFFF'` with `// on-gradient` comment
8. Keep star rating gold color from `STAR_RATING_COLOR` import from palette.ts

**(tabs)/index.tsx** (5 hex values):
1. Replace skeleton colors with `colors.skeleton`
2. Replace RefreshControl tintColor with `colors.tint`
3. Replace any button/text colors with tokens

**(tabs)/search.tsx** (7 hex values):
1. Replace search bar background with `colors.inputBg`
2. Replace filter toggle colors with `colors.tint`, `colors.tintBg`
3. Replace text/placeholder colors with `colors.text`, `colors.placeholder`

**(tabs)/profile.tsx** (19 hex values):
1. This file has heavy isDark ternaries — most should map directly to tokens. Systematically check each ternary:
   - `isDark ? '#F0EDE6' : '#1A1A18'` -> `colors.text`
   - `isDark ? '#161614' : '#F0EDE8'` -> `colors.card`
   - `isDark ? '#0C0C0A' : '#FFFFFF'` -> `colors.background`
   - And similar patterns from RESEARCH.md Pattern 3
2. Replace all remaining hardcoded colors with tokens

**(tabs)/cookbook.tsx** (1 hex value):
1. Replace heart icon color with `colors.tint` or `colors.error` as appropriate

**(tabs)/_layout.tsx** (2 hex values):
1. Replace tab bar tint colors with `colors.tint`, `colors.tabIconDefault`

**settings.tsx** (27 hex values):
1. This is the second-largest file. It's heavily light-mode-only.
2. Replace background with `colors.background`
3. Replace section headers with `colors.surface`
4. Replace text colors with `colors.text`, `colors.textSub`, `colors.textSecondary`
5. Replace borders with `colors.border`, `colors.separator`
6. Replace toggle/switch tint with `colors.tint`
7. Replace input backgrounds with `colors.inputBg`
8. Add `useAppTheme()` for layout styles if not already present

For ALL files:
- If `useAppTheme` is already imported but not used for all color styles, extend usage
- Do NOT modify non-color properties in StyleSheet
- Do NOT change component logic/structure — only color values
- Gradient palettes stay as palette.ts imports (not themed)
  </action>
  <verify>
    <automated>cd /Users/sado/Documents/Projects/TheCook/TheCook && npx tsc --noEmit 2>&1 | head -20 && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>All 7 core screen files use theme tokens exclusively for non-decorative colors. CATEGORY_GRADIENTS and STEP_PASTEL_BACKGROUNDS imported from palette.ts in recipe/[id].tsx. All isDark ternaries that map to existing tokens eliminated. Settings screen fully supports dark mode. TypeScript compiles cleanly, all tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero TypeScript errors
- `npx jest --no-coverage` — all tests pass (including token parity from Plan 01)
- `grep -r 'CATEGORY_GRADIENTS' TheCook/ --include="*.tsx" | grep -v palette.ts | grep -v node_modules | grep -v __tests__` — zero results (no inline duplicates)
- Visual: recipe cards in dark mode should show visible card boundary against dark background (UX-01)
- `bash scripts/audit-colors.sh` — significantly reduced violation count (remaining violations will be in files swept by Plan 03)
</verification>

<success_criteria>
- Recipe cards (grid + row) have `colors.cardBorder` + `borderWidth: isDark ? 1 : 0` — satisfying UX-01 dark mode contrast
- Recipe cards (grid + row) have `colors.card` background — satisfying UX-01 light mode contrast
- All 11 files compile without TypeScript errors
- CATEGORY_GRADIENTS referenced only via palette.ts import across card and detail files
- isDark ternaries eliminated where theme tokens provide the same values
- Audit script shows reduced violation count compared to Plan 01 baseline
</success_criteria>

<output>
After completion, create `.planning/phases/14-color-token-sweep/14-02-SUMMARY.md`
</output>
