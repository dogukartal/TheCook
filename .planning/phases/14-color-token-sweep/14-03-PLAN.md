---
phase: 14-color-token-sweep
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - TheCook/components/cooking/step-content.tsx
  - TheCook/components/cooking/sefim-sheet.tsx
  - TheCook/components/cooking/ingredients-sheet.tsx
  - TheCook/components/cooking/completion-screen.tsx
  - TheCook/components/cooking/circular-timer.tsx
  - TheCook/components/cooking/progress-bar.tsx
  - TheCook/components/cooking/resume-banner.tsx
  - TheCook/components/cooking/timer-indicator.tsx
  - TheCook/app/recipe/cook/[id].tsx
  - TheCook/src/components/search/CategoryStrip.tsx
  - TheCook/src/components/search/FilterPanel.tsx
  - TheCook/components/discovery/category-filter.tsx
  - TheCook/components/discovery/ingredient-chips.tsx
  - TheCook/components/ui/chip.tsx
  - TheCook/components/recipe/serving-stepper.tsx
  - TheCook/components/themed-text.tsx
  - TheCook/app/onboarding/skill-level.tsx
  - TheCook/app/onboarding/equipment.tsx
  - TheCook/app/onboarding/allergens.tsx
  - TheCook/app/onboarding/account-nudge.tsx
  - TheCook/app/(auth)/sign-in.tsx
  - TheCook/app/(auth)/sign-up.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Cooking mode screens use theme tokens for all non-pastel colors"
    - "Search and discovery components use theme tokens for backgrounds, text, and borders"
    - "Onboarding screens support dark mode via theme tokens"
    - "Auth screens use theme tokens for layout colors (Google brand blue exempt)"
    - "Zero hardcoded hex values remain in component files outside theme.ts and palette.ts"
  artifacts:
    - path: "TheCook/components/cooking/step-content.tsx"
      provides: "Theme-tokenized step content with palette.ts pastel import"
      contains: "STEP_PASTEL_BACKGROUNDS"
    - path: "TheCook/components/cooking/sefim-sheet.tsx"
      provides: "Theme-tokenized Sefim sheet"
      contains: "colors.surface"
    - path: "TheCook/app/onboarding/skill-level.tsx"
      provides: "Dark mode enabled onboarding screen"
      contains: "colors.background"
    - path: "TheCook/app/(auth)/sign-in.tsx"
      provides: "Theme-tokenized auth screen (Google blue exempt)"
      contains: "GOOGLE_BRAND_BLUE"
  key_links:
    - from: "TheCook/components/cooking/step-content.tsx"
      to: "TheCook/constants/palette.ts"
      via: "import STEP_PASTEL_BACKGROUNDS"
      pattern: "import.*palette"
    - from: "TheCook/app/(auth)/sign-in.tsx"
      to: "TheCook/constants/palette.ts"
      via: "import GOOGLE_BRAND_BLUE"
      pattern: "import.*GOOGLE_BRAND_BLUE"
    - from: "scripts/audit-colors.sh"
      to: "TheCook/"
      via: "exit code 0 (zero violations)"
      pattern: "exit 0"
---

<objective>
Sweep all remaining hardcoded hex/rgba values from cooking flow, search/discovery, UI primitives, onboarding, and auth screens, replacing them with theme tokens and palette imports to achieve zero hardcoded hex values in component files.

Purpose: This plan completes the color token sweep by covering the remaining 22 files. After this plan, the audit script should report zero violations — every color in the app flows through the theme system (success criterion 1).

Output: 22 files swept clean, audit script passes with zero violations.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-color-token-sweep/14-RESEARCH.md
@.planning/phases/14-color-token-sweep/14-01-SUMMARY.md

<interfaces>
<!-- Theme tokens available (from Plan 01) -->
From TheCook/constants/theme.ts:
```typescript
// All tokens accessible via colors.X from useAppTheme():
text, background, tint, icon, tabIconDefault, tabIconSelected,
surface, card, border, textSub, textMuted,
tintBg, onTint, textSecondary, cardBorder, shadow,
success, successBg, warning, warningBg, error, errorBg,
disabledIcon, overlay, skeleton, placeholder, inputBg, separator
```

From TheCook/constants/palette.ts:
```typescript
export const CATEGORY_GRADIENTS: Record<string, [string, string]>;
export const DEFAULT_GRADIENT: [string, string];
export const STEP_PASTEL_BACKGROUNDS: string[];
export const CATEGORY_STRIP_COLORS: Record<string, string>;
export const STAR_RATING_COLOR: string;
export const GOOGLE_BRAND_BLUE: string;
```

From TheCook/contexts/ThemeContext.tsx:
```typescript
export function useAppTheme(): { isDark: boolean; colors: typeof Colors.light; toggleTheme: () => void };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep cooking flow + search + UI primitives (16 files)</name>
  <files>TheCook/components/cooking/step-content.tsx, TheCook/components/cooking/sefim-sheet.tsx, TheCook/components/cooking/ingredients-sheet.tsx, TheCook/components/cooking/completion-screen.tsx, TheCook/components/cooking/circular-timer.tsx, TheCook/components/cooking/progress-bar.tsx, TheCook/components/cooking/resume-banner.tsx, TheCook/components/cooking/timer-indicator.tsx, TheCook/app/recipe/cook/[id].tsx, TheCook/src/components/search/CategoryStrip.tsx, TheCook/src/components/search/FilterPanel.tsx, TheCook/components/discovery/category-filter.tsx, TheCook/components/discovery/ingredient-chips.tsx, TheCook/components/ui/chip.tsx, TheCook/components/recipe/serving-stepper.tsx, TheCook/components/themed-text.tsx</files>
  <action>
Read each file first, then apply the standard sweep pattern: replace hardcoded hex/rgba -> theme token or palette import.

**COOKING FLOW (9 files):**

**step-content.tsx** (20 hex values):
1. Remove inline `STEP_PASTEL_BACKGROUNDS` / `STEP_PASTEL_COLORS` array. Import from `@/constants/palette`.
2. Replace status colors: success green -> `colors.success`/`colors.successBg`, warning amber -> `colors.warning`/`colors.warningBg`, error red -> `colors.error`/`colors.errorBg`
3. Replace text colors -> `colors.text`, `colors.textSub`, `colors.textSecondary`
4. Replace backgrounds -> `colors.background`, `colors.surface`, `colors.card`
5. Pastel step backgrounds stay as `STEP_PASTEL_BACKGROUNDS[index % 8]` from palette import
6. Checkpoint callout green bg -> `colors.successBg`, text -> `colors.success`
7. Warning callout amber bg -> `colors.warningBg`, text -> `colors.warning`

**sefim-sheet.tsx** (12 hex values):
1. Replace sheet background with `colors.surface` or `colors.card`
2. Replace text colors with tokens
3. Brand/tint colors -> `colors.tint`
4. Overlay backdrop -> `colors.overlay`
5. Input background -> `colors.inputBg`
6. Border -> `colors.border`

**ingredients-sheet.tsx** (5 hex values):
1. Sheet background -> `colors.surface`
2. Swap button colors -> `colors.tint`, `colors.tintBg`
3. Text -> `colors.text`, `colors.textSub`
4. Overlay -> `colors.overlay`

**completion-screen.tsx** (5 hex values):
1. Star rating gold -> import `STAR_RATING_COLOR` from palette.ts (decorative, not themed)
2. Background -> `colors.background`
3. Text -> `colors.text`, `colors.textSub`
4. Brand/tint buttons -> `colors.tint`, `colors.onTint`

**circular-timer.tsx** (2 hex values):
1. Timer arc active color -> `colors.tint`
2. Timer arc track color -> `colors.border` or `colors.surface`

**progress-bar.tsx** (1 hex value):
1. Active step color -> `colors.tint`

**resume-banner.tsx** (6 hex values):
1. Banner background -> `colors.tintBg` or `colors.card`
2. Text -> `colors.text`, `colors.textSub`
3. Button -> `colors.tint`, `colors.onTint`
4. Border -> `colors.border`

**timer-indicator.tsx** (3 hex values):
1. Badge background -> `colors.warningBg` or `colors.tintBg`
2. Badge text -> `colors.warning` or `colors.tint`
3. Any remaining colors -> appropriate tokens

**app/recipe/cook/[id].tsx** (5 hex values):
1. Sefim button -> `colors.tint`
2. Background -> `colors.background`
3. Text -> `colors.text`
4. Any remaining -> appropriate tokens

**SEARCH / DISCOVERY (4 files):**

**CategoryStrip.tsx** (8 hex values):
1. Import `CATEGORY_STRIP_COLORS` from palette.ts if category-specific colors are used
2. Chip backgrounds (active/inactive) -> `colors.tintBg` / `colors.surface`
3. Text -> `colors.text`, active text -> `colors.tint`
4. Category-specific accent colors stay from palette import

**FilterPanel.tsx** (5 hex values):
1. Replace light-only styles with theme tokens
2. Background -> `colors.surface`
3. Chip colors -> `colors.tint`/`colors.tintBg` for active, `colors.surface` for inactive
4. Text -> `colors.text`, `colors.textSub`

**category-filter.tsx** (4 hex values):
1. Replace light-only gray palette with tokens
2. Chip backgrounds -> `colors.surface`, active -> `colors.tintBg`
3. Text -> `colors.text`

**ingredient-chips.tsx** (6 hex values):
1. Chip background -> `colors.tintBg`
2. Chip text -> `colors.tint`
3. Remove button -> `colors.textMuted`

**UI PRIMITIVES (3 files):**

**chip.tsx** (8 hex values):
1. Selected state background -> `colors.tintBg`, text -> `colors.tint`
2. Unselected background -> `colors.surface`, text -> `colors.text`
3. Border -> `colors.border`

**serving-stepper.tsx** (7 hex values):
1. Stepper button bg -> `colors.tintBg`, icon -> `colors.tint`
2. Disabled state -> `colors.disabledIcon`
3. Value text -> `colors.text`
4. Border -> `colors.border`

**themed-text.tsx** (1 hex value):
1. Link color -> `colors.tint`

For ALL files:
- Ensure `useAppTheme` is imported and destructured if not already
- Move color-dependent values from StyleSheet.create to inline style arrays
- Do NOT change component logic, layout, or non-color properties
- Do NOT theme-switch decorative palettes (step pastels, category gradients, star gold)
  </action>
  <verify>
    <automated>cd /Users/sado/Documents/Projects/TheCook/TheCook && npx tsc --noEmit 2>&1 | head -20 && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>All 16 cooking/search/UI primitive files use theme tokens exclusively. STEP_PASTEL_BACKGROUNDS imported from palette.ts in step-content.tsx. Status colors (success/warning/error) use semantic tokens. No hardcoded hex values remain in these files except palette.ts imports.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep onboarding + auth screens and run final audit (6 files)</name>
  <files>TheCook/app/onboarding/skill-level.tsx, TheCook/app/onboarding/equipment.tsx, TheCook/app/onboarding/allergens.tsx, TheCook/app/onboarding/account-nudge.tsx, TheCook/app/(auth)/sign-in.tsx, TheCook/app/(auth)/sign-up.tsx</files>
  <action>
Read each file first. These are all currently light-mode-only with heavy StyleSheet hardcoding and minimal/no `useAppTheme()` usage for layout styles. The goal: full dark mode parity per RESEARCH.md recommendation.

**ONBOARDING (4 files):**

**skill-level.tsx** (13 hex values):
1. Add/extend `useAppTheme()` destructure if not present for layout styles
2. Background -> `colors.background`
3. Card/option backgrounds -> `colors.card` or `colors.surface`
4. Selected option -> `colors.tintBg` border/bg, `colors.tint` text
5. Text -> `colors.text`, `colors.textSub`, `colors.textSecondary`
6. Continue button -> `colors.tint` bg, `colors.onTint` text
7. Skip button text -> `colors.textMuted`
8. Move all color styles from StyleSheet to inline arrays

**equipment.tsx** (14 hex values):
1. Same pattern as skill-level
2. Equipment grid item bg -> `colors.surface`
3. Selected item -> `colors.tintBg`, `colors.tint`
4. Icon colors -> `colors.text` for default, `colors.tint` for selected
5. Progress/count text -> `colors.textSub`

**allergens.tsx** (8 hex values):
1. Same pattern as skill-level
2. Chip styles should align with chip.tsx tokens
3. Warning/safety text -> `colors.textSecondary`

**account-nudge.tsx** (8 hex values):
1. Background -> `colors.background`
2. Card -> `colors.card` or `colors.surface`
3. Brand button -> `colors.tint`, `colors.onTint`
4. Skip text -> `colors.textMuted`

**AUTH (2 files):**

**sign-in.tsx** (14 hex values):
1. Background -> `colors.background`
2. Text -> `colors.text`, `colors.textSub`
3. Input backgrounds -> `colors.inputBg`
4. Input border -> `colors.border`
5. Input text -> `colors.text`, placeholder -> `colors.placeholder`
6. Brand button -> `colors.tint`, `colors.onTint`
7. Google button: import `GOOGLE_BRAND_BLUE` from palette.ts — this is brand-mandated and must NOT be themed. Add `// palette-exempt` comment.
8. Error text -> `colors.error`
9. Link/toggle text -> `colors.tint`
10. Divider line -> `colors.separator`

**sign-up.tsx** (18 hex values):
1. Same pattern as sign-in
2. Background -> `colors.background`
3. Form input styles -> `colors.inputBg`, `colors.border`, `colors.text`, `colors.placeholder`
4. Password confirmation match indicator -> `colors.success`
5. Error text -> `colors.error`
6. Submit button -> `colors.tint`, `colors.onTint`
7. Toggle text (sign-in link) -> `colors.tint`
8. If there is a specific auth brand color (#E8612C), check if it's close enough to `colors.tint` (#E8834A) to use the token instead. If visually distinct, add it to palette.ts as `AUTH_BRAND_COLOR` with palette-exempt comment.

**FINAL AUDIT:**
After all 6 files are swept, run `bash scripts/audit-colors.sh` to verify zero violations. If any violations remain:
1. Check if they are legitimate exemptions (brand colors on gradient, etc.)
2. Either add `// palette-exempt` comment or replace with token
3. Re-run until audit passes (exit 0)

Also run `npx jest --no-coverage` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /Users/sado/Documents/Projects/TheCook/TheCook && npx tsc --noEmit 2>&1 | head -20 && npx jest --no-coverage 2>&1 | tail -5 && bash scripts/audit-colors.sh && echo "AUDIT PASSED: zero violations"</automated>
  </verify>
  <done>All 6 onboarding/auth files support dark mode via theme tokens. Google brand blue imported from palette.ts. Audit script returns exit 0 (zero hardcoded hex violations across entire codebase). All tests pass. Phase 14 success criterion 1 achieved: "Zero hardcoded hex color values remain in component files outside theme.ts and intentional gradient palettes."</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero TypeScript errors
- `npx jest --no-coverage` — all tests pass
- `bash scripts/audit-colors.sh` — exits 0 (zero violations)
- `grep -r 'STEP_PASTEL' TheCook/ --include="*.tsx" | grep -v palette.ts | grep -v node_modules` — zero results
- Onboarding screens in dark mode: no jarring white flash, all elements readable
- Auth screens in dark mode: layout colors follow theme, Google blue unchanged
</verification>

<success_criteria>
- All 22 files use theme tokens for non-decorative colors
- Audit script returns exit code 0: zero hardcoded hex violations
- Onboarding and auth screens have full dark mode support
- Cooking mode status callouts use semantic success/warning/error tokens
- STEP_PASTEL_BACKGROUNDS and STAR_RATING_COLOR imported from palette.ts
- GOOGLE_BRAND_BLUE imported from palette.ts (not hardcoded inline)
- Phase 14 SC-1: "Zero hardcoded hex color values remain in component files outside theme.ts and intentional gradient palettes"
</success_criteria>

<output>
After completion, create `.planning/phases/14-color-token-sweep/14-03-SUMMARY.md`
</output>
