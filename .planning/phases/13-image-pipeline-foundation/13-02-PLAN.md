---
phase: 13-image-pipeline-foundation
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - TheCook/.gitignore
  - TheCook/app.json
autonomous: false
requirements:
  - IMG-04

must_haves:
  truths:
    - "Raw source images are gitignored to keep repo size small"
    - "Optimized WebP images in app/assets/images/ are committed and bundled"
    - "A production export build resolves the bundled recipe images without errors"
  artifacts:
    - path: "TheCook/.gitignore"
      provides: "Gitignore rules for raw source images"
      contains: "content/images/raw"
    - path: "TheCook/app/assets/images/menemen-cover.webp"
      provides: "Optimized cover image included in production bundle"
  key_links:
    - from: "TheCook/app.json"
      to: "TheCook/app/assets/images/*.webp"
      via: "assetBundlePatterns includes app/assets/**"
      pattern: "app/assets"
    - from: "TheCook/app/assets/image-registry.ts"
      to: "TheCook/app/assets/images/menemen-cover.webp"
      via: "require() resolved by Metro at bundle time"
      pattern: "require.*menemen-cover\\.webp"
---

<objective>
Configure git and asset bundling for the image pipeline, then verify the complete pipeline works in a production export build.

Purpose: The pipeline is only valuable if images actually resolve in production. This plan ensures the raw/optimized file split is correct, gitignore is configured, and a production export confirms Metro bundles the WebP images.
Output: Correct gitignore, verified production export with images.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-image-pipeline-foundation/13-RESEARCH.md
@.planning/phases/13-image-pipeline-foundation/13-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From TheCook/app/assets/image-registry.ts (auto-generated):
```typescript
export type ImageSource = number;
export interface RecipeImages {
  cover: ImageSource | null;
  steps: (ImageSource | null)[];
}
export function getRecipeImages(recipeId: string): RecipeImages;
```

From TheCook/app.json:
```json
{
  "assetBundlePatterns": ["assets/**", "app/assets/**"]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure gitignore and verify asset bundling setup</name>
  <files>TheCook/.gitignore</files>
  <action>
    1. Update TheCook/.gitignore to add rules for the image pipeline:
       - Add `content/images/raw/` to gitignore (raw source PNGs are large, ~2MB each, should not bloat the repo)
       - Do NOT gitignore `app/assets/images/` (optimized WebP files MUST be committed -- Metro needs them at bundle time)
       - Do NOT gitignore `app/assets/image-registry.ts` (committed build artifact -- Metro needs it at bundle time)
       - Add a comment explaining the split: raw images are gitignored, optimized outputs are committed

    2. Verify app.json assetBundlePatterns already covers the images directory:
       - Current patterns: `["assets/**", "app/assets/**"]`
       - `app/assets/**` already covers `app/assets/images/*.webp` -- no changes needed to app.json
       - Log confirmation, do not modify app.json unless the pattern is missing

    3. Verify the optimized image is trackable by git:
       - `git add TheCook/app/assets/images/menemen-cover.webp` should work (not blocked by gitignore)
       - `git add TheCook/app/assets/image-registry.ts` should work

    4. Run `npx jest -x` to confirm no test regressions from gitignore changes
  </action>
  <verify>
    <automated>cd TheCook && grep -q "content/images/raw" .gitignore && npx jest -x</automated>
  </verify>
  <done>
    - content/images/raw/ is in .gitignore
    - app/assets/images/ is NOT in .gitignore
    - app/assets/image-registry.ts is NOT in .gitignore
    - app.json assetBundlePatterns covers app/assets/**
    - All tests pass
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify production export bundles images correctly</name>
  <action>
    Run production export build and verify Metro bundles the WebP images without errors. This is the critical validation that the entire pipeline (raw PNG -> WebP -> registry -> Metro bundle) works end-to-end.
  </action>
  <what-built>
    Complete image pipeline: build-images.ts converts raw PNGs to optimized WebP, generates image-registry.ts with static require() calls, wired into prebuild chain, menemen recipe has a real cover image.
  </what-built>
  <how-to-verify>
    1. Run production export to verify Metro bundles the images:
       ```
       cd TheCook && npx expo export --platform ios
       ```
    2. Check the dist output contains WebP assets:
       ```
       ls dist/assets/ | grep -i webp
       ```
       (or look for hashed asset files -- Metro hashes filenames in export)
    3. Verify no Metro bundler errors about missing images or unresolved require()
    4. Verify the export completes successfully (exit code 0)
    5. Optional: Run `npx expo start` and navigate to a screen to confirm no runtime errors (Phase 15 will actually render images, but the app should not crash)
  </how-to-verify>
  <verify>
    <automated>cd TheCook && npx expo export --platform ios 2>&1 | tail -5</automated>
  </verify>
  <done>
    - Production export completes without image resolution errors
    - dist/assets/ contains bundled image assets
    - Human confirmed: pipeline works end-to-end
  </done>
  <resume-signal>Type "approved" if export succeeded and assets are present, or describe any errors</resume-signal>
</task>

</tasks>

<verification>
1. `grep "content/images/raw" TheCook/.gitignore` -- raw images gitignored
2. `cd TheCook && npm run prebuild` -- full pipeline succeeds
3. `cd TheCook && npx expo export --platform ios` -- production export resolves images
4. `cd TheCook && npx jest` -- full test suite green
</verification>

<success_criteria>
- Git is configured to ignore raw source images but commit optimized WebP outputs
- Production export build (npx expo export) completes without image resolution errors
- The full prebuild chain (build-images -> build-recipes) runs cleanly
- Human verified: production build contains bundled WebP image assets
</success_criteria>

<output>
After completion, create `.planning/phases/13-image-pipeline-foundation/13-02-SUMMARY.md`
</output>
