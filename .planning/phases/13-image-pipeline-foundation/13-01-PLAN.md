---
phase: 13-image-pipeline-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TheCook/package.json
  - TheCook/scripts/build-images.ts
  - TheCook/app/assets/image-registry.ts
  - TheCook/app/assets/images/menemen-cover.webp
  - TheCook/content/images/raw/menemen-cover.png
  - TheCook/__tests__/build-images.test.ts
  - TheCook/__tests__/image-registry.test.ts
autonomous: true
requirements:
  - IMG-04

must_haves:
  truths:
    - "Running build-images produces WebP files under 100KB in app/assets/images/"
    - "The generated image-registry.ts maps recipe IDs to static require() calls"
    - "getRecipeImages returns cover and steps for recipes with images, and null fallbacks for recipes without"
  artifacts:
    - path: "TheCook/scripts/build-images.ts"
      provides: "Image optimization pipeline and registry generation"
      min_lines: 80
    - path: "TheCook/app/assets/image-registry.ts"
      provides: "Static require() map for Metro bundling"
      exports: ["getRecipeImages", "RecipeImages", "ImageSource"]
    - path: "TheCook/app/assets/images/menemen-cover.webp"
      provides: "Optimized WebP cover image for menemen"
    - path: "TheCook/__tests__/build-images.test.ts"
      provides: "Build script unit tests"
      min_lines: 30
    - path: "TheCook/__tests__/image-registry.test.ts"
      provides: "Registry lookup tests"
      min_lines: 20
  key_links:
    - from: "TheCook/scripts/build-images.ts"
      to: "TheCook/app/assets/images/*.webp"
      via: "sharp conversion pipeline"
      pattern: "sharp.*\\.webp"
    - from: "TheCook/scripts/build-images.ts"
      to: "TheCook/app/assets/image-registry.ts"
      via: "fs.writeFileSync auto-generation"
      pattern: "writeFileSync.*image-registry"
    - from: "TheCook/app/assets/image-registry.ts"
      to: "TheCook/app/assets/images/*.webp"
      via: "static require() calls"
      pattern: "require.*images.*\\.webp"
---

<objective>
Create the build-time image pipeline that converts raw PNG/JPG source images into optimized WebP files and auto-generates a TypeScript image registry with static require() calls for Metro bundling.

Purpose: Metro requires compile-time constant paths for require(). This plan builds the complete pipeline from raw source images through to a consumable registry, including the first real image (menemen cover).
Output: Working build-images.ts script, auto-generated image-registry.ts, optimized menemen-cover.webp, and passing tests.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-image-pipeline-foundation/13-RESEARCH.md

<interfaces>
<!-- Existing build pipeline contracts the executor needs -->

From TheCook/src/types/recipe.ts:
```typescript
export const RecipeSchema = z.object({
  id: z.string().min(1),
  coverImage: z.string().nullable().default(null),
  steps: z.array(StepSchema).min(1),
  // ... other fields
});

export const StepSchema = z.object({
  stepImage: z.string().nullable().default(null),
  // ... other fields
});
```

From TheCook/scripts/build-recipes.ts:
```typescript
// Existing pattern: reads YAML from content/recipes/, validates with Zod, outputs JSON
// Uses --dir and --out flags for testability
// Aborts on invalid recipe (process.exit(1))
```

From TheCook/package.json scripts:
```json
{
  "build-recipes": "tsx scripts/build-recipes.ts",
  "prebuild": "npm run build-recipes"
}
```

Existing test pattern (from __tests__/buildScript.test.ts):
```typescript
// Uses spawnSync to run build scripts in temp directories
// Creates temp input dirs with test fixtures, temp output dirs
// Asserts exit codes and output file contents
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install sharp, create test scaffolds, and build the image pipeline script</name>
  <files>
    TheCook/package.json,
    TheCook/__tests__/build-images.test.ts,
    TheCook/__tests__/image-registry.test.ts,
    TheCook/scripts/build-images.ts,
    TheCook/content/images/raw/menemen-cover.png,
    TheCook/app/assets/images/.gitkeep
  </files>
  <behavior>
    - Test: build-images converts a PNG fixture to WebP under 100KB (create a small test PNG in temp dir, run script, assert output file size)
    - Test: build-images skips recipes with no source images (no crash, graceful null in registry)
    - Test: build-images generates image-registry.ts with correct require() paths and getRecipeImages export
    - Test: build-images errors when YAML references an image that does not exist on disk
    - Test: getRecipeImages returns { cover: null, steps: [] } for unknown recipe ID
    - Test: getRecipeImages returns cover image source for recipe with images
  </behavior>
  <action>
    1. Install sharp: `npm install --save-dev sharp @types/sharp` (MUST be devDependencies, not dependencies -- Metro cannot bundle sharp's native bindings)

    2. Migrate one MENEMEN image to standardized naming:
       - Create `content/images/raw/` directory
       - Copy one of the 6 ChatGPT images from `content/images/MENEMEN/` to `content/images/raw/menemen-cover.png`
       - Pick the first image alphabetically (the exact choice is a content decision, any will work)

    3. Create `__tests__/build-images.test.ts` following the existing buildScript.test.ts pattern:
       - Use spawnSync to invoke `tsx scripts/build-images.ts`
       - Support `--raw-dir`, `--out-dir`, `--registry-path`, `--recipes-dir` flags for testability
       - Create a small test PNG fixture in temp dir using sharp (generate a 100x100 red square)
       - Create a minimal test YAML with coverImage pointing to the fixture
       - Assert: output WebP exists and is under 100KB
       - Assert: generated registry file contains require() for the test image
       - Assert: registry contains getRecipeImages function
       - Assert: recipe with no source image gets null in registry
       - Assert: exit code 1 when YAML references nonexistent image file

    4. Create `__tests__/image-registry.test.ts`:
       - Test getRecipeImages returns { cover: null, steps: [] } for unknown recipe ID
       - Test that the registry module exports the expected interface (getRecipeImages, RecipeImages type)
       - Note: these tests can only run AFTER build-images generates the registry. Use a simple mock or test the generated file's exports.

    5. Create `scripts/build-images.ts`:
       - Accept flags: `--raw-dir` (default: content/images/raw), `--out-dir` (default: app/assets/images), `--registry-path` (default: app/assets/image-registry.ts), `--recipes-dir` (default: content/recipes)
       - Read all YAML files from recipes dir, extract recipe IDs
       - For each recipe ID, look for `{recipe-id}-cover.{png,jpg,jpeg}` in raw dir
       - For each found source image: resize to max 800px width (withoutEnlargement: true), convert to WebP quality 75 effort 4, write to out-dir as `{recipe-id}-cover.webp`
       - Warn (console.warn) if any output file exceeds 100KB (do not fail -- allow manual review)
       - Look for `{recipe-id}-step-{NN}.{png,jpg,jpeg}` pattern for step images (NN = 01, 02, etc.)
       - Generate `image-registry.ts` with:
         - Header comment: "AUTO-GENERATED by scripts/build-images.ts -- DO NOT EDIT"
         - ImageSource type alias (number -- Metro require returns asset ID)
         - RecipeImages interface { cover: ImageSource | null; steps: (ImageSource | null)[] }
         - Registry object mapping each recipe ID to its RecipeImages (static require() for found images, null for missing)
         - getRecipeImages(recipeId: string) function with null fallback
       - Read YAML coverImage fields; if a YAML has coverImage set to a filename, validate that file exists in out-dir (error if not)
       - Print summary: "Processed X images for Y recipes, Z warnings"

    6. Create `app/assets/images/.gitkeep` as placeholder for the images output directory

    7. Run the build script: `npx tsx scripts/build-images.ts` from TheCook/ directory to generate the actual registry and optimized images

    8. Run tests to confirm GREEN
  </action>
  <verify>
    <automated>cd TheCook && npx jest __tests__/build-images.test.ts __tests__/image-registry.test.ts -x</automated>
  </verify>
  <done>
    - sharp installed in devDependencies
    - menemen-cover.png exists in content/images/raw/
    - menemen-cover.webp exists in app/assets/images/ and is under 100KB
    - image-registry.ts is auto-generated with getRecipeImages function
    - getRecipeImages("menemen") returns { cover: require("./images/menemen-cover.webp"), steps: [...] }
    - getRecipeImages("unknown") returns { cover: null, steps: [] }
    - All build-images and image-registry tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire YAML coverImage field and integrate into prebuild pipeline</name>
  <files>
    TheCook/content/recipes/menemen.yaml,
    TheCook/package.json
  </files>
  <action>
    1. Update `content/recipes/menemen.yaml`: change `coverImage: null` to `coverImage: menemen-cover.webp`
       - This is the YAML-to-registry contract: coverImage value matches the optimized filename in app/assets/images/
       - The build-images.ts script validates this reference exists

    2. Update `package.json` scripts to chain build-images before build-recipes in the prebuild step:
       - Add new script: `"build-images": "tsx scripts/build-images.ts"`
       - Update prebuild: `"prebuild": "npm run build-images && npm run build-recipes"`
       - This ensures images are always optimized and registry is always current before recipe JSON is built

    3. Run the full prebuild to verify the chain works:
       - `npm run prebuild` should:
         a. Run build-images (convert PNGs, generate registry)
         b. Run build-recipes (validate YAML including coverImage, produce recipes.json)
       - Both should exit 0

    4. Rebuild recipes.json so it contains the updated coverImage field value for menemen

    5. Run the full test suite to verify no regressions: `npx jest`
  </action>
  <verify>
    <automated>cd TheCook && npm run prebuild && npx jest -x</automated>
  </verify>
  <done>
    - menemen.yaml has coverImage: menemen-cover.webp (not null)
    - npm run prebuild runs build-images then build-recipes without error
    - recipes.json contains coverImage: "menemen-cover.webp" for menemen recipe
    - image-registry.ts is regenerated as part of prebuild
    - Full test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
1. `cd TheCook && npx jest -x` -- all tests pass including new build-images and image-registry tests
2. `cd TheCook && npm run prebuild` -- full pipeline runs without error
3. `ls -la TheCook/app/assets/images/menemen-cover.webp` -- file exists and is under 100KB
4. `grep "menemen" TheCook/app/assets/image-registry.ts` -- contains require() for menemen cover
5. `grep "coverImage" TheCook/content/recipes/menemen.yaml` -- shows menemen-cover.webp (not null)
6. `grep "sharp" TheCook/package.json` -- appears only in devDependencies
</verification>

<success_criteria>
- Build script converts raw PNGs to WebP under 100KB
- Image registry auto-generated with static require() calls
- getRecipeImages API provides null-safe lookup for all 30 recipes
- Menemen recipe has a real cover image flowing through the full pipeline
- Prebuild chain (build-images -> build-recipes) runs end-to-end
- All existing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/13-image-pipeline-foundation/13-01-SUMMARY.md`
</output>
