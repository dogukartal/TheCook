---
phase: 07-foundation-pivot
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - TheCook/src/hooks/useFeedScreen.ts
  - TheCook/src/hooks/useSearchScreen.ts
  - TheCook/src/hooks/useCookbookScreen.ts
  - TheCook/app/(tabs)/index.tsx
  - TheCook/app/(tabs)/search.tsx
  - TheCook/app/(tabs)/my-kitchen.tsx
autonomous: true
requirements: [DISC-05]

must_haves:
  truths:
    - "Feed screen data orchestration lives in useFeedScreen hook, not in index.tsx"
    - "Search screen data orchestration lives in useSearchScreen hook, not in search.tsx"
    - "Cookbook screen data orchestration lives in useCookbookScreen hook, not in my-kitchen.tsx"
    - "Screen files are thin shells — call hook, render components, define styles"
    - "All screens pass HardFilter to query functions (skill + equipment + allergen)"
    - "Cookbook bookmark query uses getBookmarkedRecipes with allergen exclusion"
  artifacts:
    - path: "TheCook/src/hooks/useFeedScreen.ts"
      provides: "Feed screen data hook"
      exports: ["useFeedScreen", "FeedScreenState", "FeedScreenActions"]
    - path: "TheCook/src/hooks/useSearchScreen.ts"
      provides: "Search screen data hook"
      exports: ["useSearchScreen", "SearchScreenState", "SearchScreenActions"]
    - path: "TheCook/src/hooks/useCookbookScreen.ts"
      provides: "Cookbook screen data hook"
      exports: ["useCookbookScreen", "CookbookScreenState", "CookbookScreenActions"]
  key_links:
    - from: "TheCook/src/hooks/useFeedScreen.ts"
      to: "TheCook/src/db/recipes.ts"
      via: "getAllRecipesForFeed with HardFilter"
      pattern: "getAllRecipesForFeed"
    - from: "TheCook/src/hooks/useCookbookScreen.ts"
      to: "TheCook/src/db/recipes.ts"
      via: "getBookmarkedRecipes with HardFilter (DISC-05)"
      pattern: "getBookmarkedRecipes"
    - from: "TheCook/app/(tabs)/index.tsx"
      to: "TheCook/src/hooks/useFeedScreen.ts"
      via: "const state = useFeedScreen()"
      pattern: "useFeedScreen"
---

<objective>
Extract data orchestration from all three screen files into typed hooks in `src/hooks/`. Screens become thin rendering shells. Inline bookmark SQL in my-kitchen.tsx replaced by `getBookmarkedRecipes` from Plan 01.

Purpose: Enable parallel frontend/backend development per user decision. Backend developer owns `src/hooks/` and `src/db/`, frontend developer owns screen JSX and components.
Output: Three new hook files, three refactored screen files (thin shells).
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-foundation-pivot/07-RESEARCH.md
@.planning/phases/07-foundation-pivot/07-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — the executor needs these contracts -->

From TheCook/src/types/discovery.ts (after Plan 01):
```typescript
export interface HardFilter {
  allergens: string[];
  skillLevel: string | null;
  equipment: string[];
}
```

From TheCook/src/db/recipes.ts (after Plan 01 — new signatures):
```typescript
export async function getAllRecipesForFeed(db: SQLiteDatabase, filter: HardFilter): Promise<RecipeListItem[]>
export async function getAllRecipesForSearch(db: SQLiteDatabase, filter: HardFilter): Promise<(RecipeListItem & { ingredient_groups: string })[]>
export async function queryRecipesByFilter(db: SQLiteDatabase, filter: DiscoveryFilter, hardFilter: HardFilter): Promise<RecipeListItem[]>
export async function getBookmarkedRecipes(db: SQLiteDatabase, bookmarkIds: string[], hardFilter: HardFilter): Promise<RecipeListItem[]>
export function useRecipesDb(): { ... } // hook with updated signatures
```

From TheCook/src/types/profile.ts (after Plan 01):
```typescript
export const ProfileSchema = z.object({
  allergens: ..., skillLevel: ..., equipment: ...,
  onboardingCompleted: ..., accountNudgeShown: ...,
  cuisinePreferences: z.string().nullable().default(null),
  appGoals: z.string().nullable().default(null),
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract useFeedScreen and useSearchScreen hooks</name>
  <files>
    TheCook/src/hooks/useFeedScreen.ts,
    TheCook/src/hooks/useSearchScreen.ts,
    TheCook/app/(tabs)/index.tsx,
    TheCook/app/(tabs)/search.tsx
  </files>
  <action>
    **useFeedScreen.ts:**
    1. Create `TheCook/src/hooks/useFeedScreen.ts`.
    2. Define and export `FeedScreenState` interface containing all state from current index.tsx:
       - `profile: Profile | null`, `profileLoaded: boolean`, `recipes: RecipeListItem[]`, `loading: boolean`
       - `activeTab: FeedTab`, `selectedCategory: Category | null`, `bookmarkedIds: Set<string>`
       - `resumeSession: CookingSession | null`, `resumeRecipeName: string`, `resumeTotalSteps: number`
       - `refreshing: boolean`
    3. Define and export `FeedScreenActions` interface:
       - `setActiveTab: (tab: FeedTab) => void`
       - `setSelectedCategory: (cat: Category | null) => void`
       - `handleBookmarkToggle: (id: string) => Promise<void>`
       - `handleRefresh: () => Promise<void>`
       - `handleResume: () => void`
       - `handleDismissResume: () => Promise<void>`
       - `handleRecipePress: (id: string) => void`
       - `loadRecipes: () => Promise<void>`
    4. Export `FeedTab` type (`'trending' | 'for-you'`).
    5. Implement `useFeedScreen()` by moving ALL useState, useEffect, useFocusEffect, useCallback, and handler functions from index.tsx. Build `HardFilter` from profile: `{ allergens: profile.allergens, skillLevel: profile.skillLevel, equipment: profile.equipment }`. Pass this to `getAllRecipesForFeed` instead of separate params.
    6. The hook must NOT import any React Native UI components or return JSX.

    **Refactor index.tsx (thin shell):**
    1. Remove all useState, useEffect, useFocusEffect, useCallback, handler functions.
    2. Import `useFeedScreen` from `@/src/hooks/useFeedScreen`.
    3. Call `const { profile, profileLoaded, recipes, loading, activeTab, selectedCategory, bookmarkedIds, resumeSession, resumeRecipeName, resumeTotalSteps, refreshing, setActiveTab, setSelectedCategory, handleBookmarkToggle, handleRefresh, handleResume, handleDismissResume, handleRecipePress, loadRecipes } = useFeedScreen();`
    4. Keep ONLY the JSX return and StyleSheet. All data references point to destructured state/actions.
    5. Remove direct imports of `useSQLiteContext`, `useRecipesDb`, `useProfileDb`, `getActiveSession`, `clearSession`, `getRecipeById`.

    **useSearchScreen.ts:**
    1. Create `TheCook/src/hooks/useSearchScreen.ts`.
    2. Define and export `SearchScreenState` interface:
       - `allIngredients: string[]`, `allRecipes: (RecipeListItem & { ingredient_groups: string })[]`
       - `profile: Profile | null`, `profileLoaded: boolean`, `recentViews: RecipeListItem[]`
       - `query: string`, `ingredientChips: string[]`, `results: RecipeListItem[]`
       - `searchLoading: boolean`, `bookmarkedIds: Set<string>`, `dropdownOpen: boolean`
       - `ingredientSuggestions: string[]`, `displayResults: RecipeListItem[]`
       - `isIdle: boolean`, `hasChips: boolean`, `showDropdown: boolean`
    3. Define and export `SearchScreenActions`:
       - `handleSelectIngredient: (name: string) => void`
       - `handleRemoveChip: (name: string) => void`
       - `handleRecipePress: (id: string) => void`
       - `handleBookmarkToggle: (id: string) => Promise<void>`
       - `handleQueryChange: (text: string) => void`
    4. Implement `useSearchScreen()` by moving ALL state + logic from search.tsx. Build `HardFilter` from profile and pass to `getAllRecipesForSearch`.
    5. Include the `ingredientSuggestions` useMemo and `displayResults` useMemo inside the hook. Also compute `isIdle`, `hasChips`, `showDropdown` inside the hook and return them.

    **Refactor search.tsx (thin shell):**
    1. Same pattern as index.tsx — remove all state/logic, import `useSearchScreen`, destructure, keep only JSX and styles.
  </action>
  <verify>
    <automated>cd TheCook && npx jest --no-coverage && npx tsc --noEmit</automated>
  </verify>
  <done>useFeedScreen and useSearchScreen hooks exist in src/hooks/. index.tsx and search.tsx are thin shells (no useState, no useEffect, no direct db imports). TypeScript compiles. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Extract useCookbookScreen hook — replaces inline SQL with getBookmarkedRecipes</name>
  <files>
    TheCook/src/hooks/useCookbookScreen.ts,
    TheCook/app/(tabs)/my-kitchen.tsx
  </files>
  <action>
    **useCookbookScreen.ts:**
    1. Create `TheCook/src/hooks/useCookbookScreen.ts`.
    2. Define and export `CookbookScreenState` interface:
       - `profile: Profile | null`, `bookmarkedIds: Set<string>`, `savedRecipes: RecipeListItem[]`
       - `loading: boolean`, `session: Session | null` (from useSession)
       - `profileSummary: string` (computed)
    3. Define and export `CookbookScreenActions`:
       - `handleBookmarkToggle: (id: string) => Promise<void>`
       - `handleRecipePress: (id: string) => void`
       - `handleSettingsPress: () => void`
       - `handleSignOut: () => Promise<void>`
       - `handleSignIn: () => void`
       - `loadData: () => Promise<void>`
    4. Implement `useCookbookScreen()`:
       - Move ALL state from my-kitchen.tsx into the hook.
       - **Critical change:** Replace the inline SQL batch SELECT (lines 81-97 of current my-kitchen.tsx) with a call to `getBookmarkedRecipes(db, bookmarkIds, hardFilter)` from `src/db/recipes.ts`. This is the key DISC-05 fix — bookmarks now filtered by allergens, skill, and equipment.
       - Build `HardFilter` from profile: `{ allergens: profile.allergens, skillLevel: profile.skillLevel, equipment: profile.equipment }`.
       - Move `buildProfileSummary()` into the hook and return it as a computed string in state.
       - Move `handleBookmarkToggle`, `handleRecipePress`, `handleSettingsPress` into the hook.
       - Include `session` and `signOut` from `useSession()` inside the hook.
    5. The hook must NOT import any React Native UI components or return JSX.

    **Refactor my-kitchen.tsx (thin shell):**
    1. Remove all useState, useCallback, useFocusEffect, inline SQL, handler functions, `ALLERGEN_LABELS`, `SKILL_LEVEL_LABELS` maps.
    2. Import `useCookbookScreen` from `@/src/hooks/useCookbookScreen`.
    3. Destructure all state and actions from the hook.
    4. Keep ONLY the JSX return and StyleSheet. Reference `session`, `profileSummary`, etc. from the hook state.
    5. Remove direct imports of `useSQLiteContext`, `useProfileDb`, `useRecipesDb`, `useSession`.

    Note: The file is still named `my-kitchen.tsx` in this plan. Plan 03 handles the rename to `cookbook.tsx`.
  </action>
  <verify>
    <automated>cd TheCook && npx jest --no-coverage && npx tsc --noEmit</automated>
  </verify>
  <done>useCookbookScreen hook exists. my-kitchen.tsx is a thin shell with no inline SQL, no direct db imports. Bookmark query now uses getBookmarkedRecipes with full hard filter (DISC-05 closed). TypeScript compiles. All tests pass.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — all type checks pass
- `npx jest --no-coverage` — full suite green
- Each screen file (index.tsx, search.tsx, my-kitchen.tsx) has zero `useState` or `useEffect` calls
- Each screen file imports only its corresponding hook from `src/hooks/`
- `getBookmarkedRecipes` is called with HardFilter (not inline SQL)
</verification>

<success_criteria>
- Three hook files exist: useFeedScreen.ts, useSearchScreen.ts, useCookbookScreen.ts
- All hooks export typed State and Actions interfaces
- Screen files are thin shells (no useState, no useEffect, no direct db/profile imports)
- Cookbook bookmark query uses getBookmarkedRecipes with allergen+skill+equipment hard filter
- Inline SQL from my-kitchen.tsx is fully extracted to recipes.ts
- TypeScript compiles, full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-foundation-pivot/07-02-SUMMARY.md`
</output>
