---
phase: 05-guided-cooking-mode
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - TheCook/app/recipe/[id].tsx
  - TheCook/components/cooking/step-content.tsx
  - TheCook/components/cooking/progress-bar.tsx
  - TheCook/components/cooking/circular-timer.tsx
  - TheCook/components/cooking/ingredients-sheet.tsx
  - TheCook/components/cooking/timer-indicator.tsx
  - TheCook/components/cooking/completion-screen.tsx
  - TheCook/components/cooking/resume-banner.tsx
  - TheCook/__tests__/step-content.test.ts
autonomous: true
requirements:
  - COOK-01
  - COOK-02
  - COOK-03
must_haves:
  truths:
    - "Recipe detail screen shows hero, metadata, ingredients, steps preview, and a Start Cooking button"
    - "Step content component renders instruction, why (tap-to-reveal), looks-like-when-done, and common mistake sections"
    - "Circular timer component displays countdown ring with play/pause"
    - "Ingredients sheet shows all recipe ingredients with checkboxes and swap buttons"
    - "Progress bar shows segmented step indicators with current position highlighted"
  artifacts:
    - path: "TheCook/app/recipe/[id].tsx"
      provides: "Cooking preview screen with Start Cooking button"
      min_lines: 200
    - path: "TheCook/components/cooking/step-content.tsx"
      provides: "Single step layout with all COOK-02/COOK-03 content"
      exports: ["StepContent"]
    - path: "TheCook/components/cooking/circular-timer.tsx"
      provides: "Circular countdown ring with play/pause button"
      exports: ["CircularTimer"]
    - path: "TheCook/components/cooking/ingredients-sheet.tsx"
      provides: "Bottom sheet with ingredient checkboxes and swap placeholders"
      exports: ["IngredientsSheet"]
    - path: "TheCook/components/cooking/progress-bar.tsx"
      provides: "Segmented progress bar"
      exports: ["SegmentedProgressBar"]
    - path: "TheCook/components/cooking/timer-indicator.tsx"
      provides: "Floating timer indicator for navigation to timer step"
      exports: ["TimerIndicator"]
    - path: "TheCook/components/cooking/completion-screen.tsx"
      provides: "Afiyet olsun celebration screen"
      exports: ["CompletionScreen"]
    - path: "TheCook/components/cooking/resume-banner.tsx"
      provides: "Resume cooking banner for feed screen"
      exports: ["ResumeBanner"]
    - path: "TheCook/__tests__/step-content.test.ts"
      provides: "Render tests for StepContent covering COOK-02 and COOK-03"
  key_links:
    - from: "TheCook/app/recipe/[id].tsx"
      to: "TheCook/app/recipe/cook/[id].tsx"
      via: "router.push on Start Cooking tap"
      pattern: "router\\.push.*cook"
    - from: "TheCook/components/cooking/step-content.tsx"
      to: "TheCook/src/types/recipe.ts"
      via: "RecipeStep type for step data"
      pattern: "RecipeStep"
---

<objective>
Replace the existing recipe detail screen with a cooking preview screen (hero, metadata, ingredients, steps preview, "Start Cooking" button) and build all reusable cooking UI components that the step-by-step view will consume.

Purpose: Separates component creation from wiring. Plan 03 wires these components into the full-screen cooking view without needing to build UI from scratch.
Output: Cooking preview screen, 7 cooking components ready for integration, StepContent render tests.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-guided-cooking-mode/05-CONTEXT.md
@.planning/phases/05-guided-cooking-mode/05-RESEARCH.md
@.planning/phases/05-guided-cooking-mode/05-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From TheCook/src/db/cooking-session.ts (created in Plan 01):
```typescript
export interface CookingSession {
  recipeId: string;
  currentStep: number;
  timerRemaining: number | null;
  timerStartTimestamp: number | null;
  ingredientChecks: number[];
  sessionStartedAt: string; // ISO 8601
}
export async function saveSession(db: SQLiteDatabase, session: CookingSession): Promise<void>;
export async function getActiveSession(db: SQLiteDatabase): Promise<CookingSession | null>;
export async function clearSession(db: SQLiteDatabase): Promise<void>;
```

From TheCook/src/hooks/useCookingTimer.ts (created in Plan 01):
```typescript
export function useCookingTimer(): {
  timer: TimerState;
  displaySeconds: number;
  start: (stepIndex: number, durationSeconds: number) => void;
  pause: () => void;
  resume: () => void;
  reset: () => void;
};
```

From TheCook/src/types/recipe.ts:
```typescript
export type Recipe = z.infer<typeof RecipeSchema>;
export type RecipeStep = z.infer<typeof StepSchema>;
// StepSchema fields: instruction, why, looksLikeWhenDone, commonMistake, recovery, stepImage, timerSeconds
```

From TheCook/src/db/recipes.ts:
```typescript
export async function getRecipeById(db: SQLiteDatabase, id: string): Promise<Recipe | null>;
```

From TheCook/app/recipe/[id].tsx (current — to be replaced):
- CATEGORY_GRADIENTS, SKILL_LABELS, ALLERGEN_LABELS constants (reuse in new preview)
- Loading and not-found states (reuse pattern)
- Bookmark toggle logic (preserve)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace recipe detail with cooking preview screen</name>
  <files>TheCook/app/recipe/[id].tsx</files>
  <action>
    Rewrite `app/recipe/[id].tsx` as the **cooking preview screen**. Preserve the existing loading/not-found patterns and bookmark toggle.

    **Layout (top to bottom):**
    1. **Hero gradient** (same CATEGORY_GRADIENTS palette): back button top-left, bookmark top-right, recipe title at bottom. Same as current but keep it.
    2. **Metadata row**: skill badge, cuisine badge, clock icon + total time, servings icon + count. Same as current.
    3. **Allergen tags row** (if allergens exist). Same as current.
    4. **Ingredients section**: "Malzemeler" header, ingredient groups with dots. Same as current.
    5. **Steps Preview section** (NEW):
       - "Adimlar" header
       - Horizontal or vertical list of step preview boxes. Each box shows:
         - Step number badge (1, 2, 3...)
         - Step title: first ~60 chars of `step.instruction` truncated with "..."
         - Pastel color background using this palette cycling by index:
           ```
           ['#FDE8D8', '#D4F0E8', '#E8DFF5', '#FFF3CD', '#D1ECF1', '#F5D5D5', '#E2F0CB', '#FCE4EC']
           ```
         - If step has `timerSeconds`, show small clock icon + formatted duration (e.g., "5 dk")
       - Step boxes are scrollable vertically within the page (not a separate horizontal scroll)
    6. **"Pismek Baslat" (Start Cooking) button** at the very bottom:
       - Full-width, brand orange (#E07B39), white text, 52px height, rounded 12px
       - Tapping navigates to `router.push(\`/recipe/cook/${id}\`)`
       - Use the cooking route path that Plan 03 will create

    **Preserve from current screen:**
    - `getRecipeById` fetch on mount
    - `recordRecentView` call
    - Bookmark toggle (addBookmark/removeBookmark)
    - Loading skeleton state
    - Not-found state
    - CATEGORY_GRADIENTS, SKILL_LABELS, ALLERGEN_LABELS constants

    **Remove from current screen:**
    - The old full step list with step cards showing instruction + hint. Replace with the Steps Preview boxes described above.
  </action>
  <verify>
    <automated>cd TheCook && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>Recipe detail screen replaced with cooking preview showing hero, metadata, ingredients, steps preview with pastel boxes, and a prominent "Start Cooking" button that navigates to cook/[id]</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build all cooking UI components with StepContent render tests</name>
  <files>
    TheCook/components/cooking/step-content.tsx,
    TheCook/components/cooking/progress-bar.tsx,
    TheCook/components/cooking/circular-timer.tsx,
    TheCook/components/cooking/ingredients-sheet.tsx,
    TheCook/components/cooking/timer-indicator.tsx,
    TheCook/components/cooking/completion-screen.tsx,
    TheCook/components/cooking/resume-banner.tsx,
    TheCook/__tests__/step-content.test.ts
  </files>
  <behavior>
    - Test: StepContent renders instruction text visible in the tree
    - Test: StepContent renders "Neden?" pressable; tapping it reveals step.why text
    - Test: StepContent renders "Gormeli" section with step.looksLikeWhenDone text
    - Test: StepContent renders "Dikkat" section with step.commonMistake text
    - Test: StepContent renders recovery text ("Ne yapmaliyim?") with step.recovery content
    - Test: StepContent does NOT render CircularTimer when step.timerSeconds is null
    - Test: StepContent renders CircularTimer area when step.timerSeconds is provided
  </behavior>
  <action>
    **Write tests FIRST (RED), then build components (GREEN).**

    **0. __tests__/step-content.test.ts** — StepContent render tests (write FIRST)
    - Use `@testing-library/react-native` (already in devDependencies from Phase 4 test setup)
    - Mock `react-native-svg` and `react-native-reanimated` (CircularTimer deps) at top of test file
    - Create a mock RecipeStep fixture:
      ```typescript
      const mockStep: RecipeStep = {
        instruction: 'Soganlari ince ince dogra',
        why: 'Ince dogranan soganlar daha cabuk karamellesir',
        looksLikeWhenDone: 'Soganlar seffaf ve altin rengi olmali',
        commonMistake: 'Soganlari cok kalin dogamak',
        recovery: 'Kalin dogramissaniz biraz daha uzun pisirin',
        stepImage: null,
        timerSeconds: null,
      };
      ```
    - Test 1: `render(<StepContent step={mockStep} stepIndex={0} totalSteps={3} />)` — assert `screen.getByText('Soganlari ince ince dogra')` exists (instruction visible)
    - Test 2: Assert `screen.getByText(/Neden/i)` exists, then `fireEvent.press()` on it, then assert `screen.getByText('Ince dogranan soganlar daha cabuk karamellesir')` exists (why revealed)
    - Test 3: Assert `screen.getByText('Soganlar seffaf ve altin rengi olmali')` exists (looksLikeWhenDone in Gormeli section)
    - Test 4: Assert `screen.getByText('Soganlari cok kalin dogamak')` exists (commonMistake in Dikkat section)
    - Test 5: Assert `screen.getByText('Kalin dogramissaniz biraz daha uzun pisirin')` exists (recovery text)
    - Test 6: With `timerSeconds: null`, assert CircularTimer is NOT rendered (query for testID="circular-timer" returns null)
    - Test 7: With `timerSeconds: 300`, assert CircularTimer area IS rendered (query for testID="circular-timer" exists)
    - Run tests — they MUST fail (RED) since StepContent does not exist yet.

    **Then build all 7 components (GREEN):**

    **1. step-content.tsx** — `StepContent` component
    Props: `{ step: RecipeStep, stepIndex: number, totalSteps: number }`
    Layout (top to bottom):
    - **Pastel color placeholder** at top (200px height), color from STEP_PASTEL_COLORS[stepIndex % 8]
    - At the image/text boundary: **step title** ("Adim {stepIndex+1}") on the left, **CircularTimer** on the right (only rendered if `step.timerSeconds` is not null). When rendered, wrap in a View with `testID="circular-timer"`. Timer props passed down.
    - **Instruction text** below — `step.instruction`, fontSize 16, color #111827, lineHeight 24
    - **"Gormeli" section** — gray label "Gormeli (You should see)", then `step.looksLikeWhenDone` text in italic, green-tinted (#15803D)
    - **"Neden?" section** — tap-to-reveal. A Pressable link text "Neden?" in brand orange. On tap, expands to show `step.why` text below it. On second tap, collapses. Use `useState<boolean>` toggle + conditional render (no animation needed).
    - **"Dikkat" section** at bottom — warning banner with light red background (#FEF2F2), red-orange left border, icon (alert-circle). Title "Dikkat!" in bold. Body: `step.commonMistake`. Below that, "Ne yapmaliyim?" label followed by `step.recovery` text.
    - Wrap everything in a ScrollView (step content may exceed screen height).

    Note: The CircularTimer is rendered here but its state is managed by the parent via useCookingTimer hook. StepContent receives timer display props: `timerDisplaySeconds`, `timerIsRunning`, `timerStepIndex`, `onTimerStart`, `onTimerPause`, `onTimerResume`. Only show timer controls when `step.timerSeconds` is not null.

    **2. progress-bar.tsx** — `SegmentedProgressBar` component
    Props: `{ totalSteps: number, currentStep: number }`
    - Horizontal row of segments (flex: 1 each), 4px height, 2px borderRadius
    - Active segments (index <= currentStep): #E07B39
    - Inactive: #E5E7EB
    - 4px gap between segments
    - 16px horizontal padding, 8px vertical padding

    **3. circular-timer.tsx** — `CircularTimer` component
    Props: `{ totalSeconds: number, displaySeconds: number, isRunning: boolean, onStart: () => void, onPause: () => void, onResume: () => void }`
    - Uses react-native-svg Circle + react-native-reanimated for smooth arc animation
    - 96x96px SVG viewBox, RADIUS=40, strokeWidth=6
    - Background circle: #E5E7EB
    - Progress circle: #E07B39, animated strokeDashoffset based on progress (displaySeconds/totalSeconds)
    - Transform rotate(-90 48 48) so arc starts from top
    - Center text: MM:SS format (Math.floor(displaySeconds/60) + ":" + pad(displaySeconds%60))
    - Below the circle: play/pause button icon. If not started (displaySeconds === totalSeconds and !isRunning): play icon, calls onStart. If running: pause icon, calls onPause. If paused (displaySeconds < totalSeconds and !isRunning): play icon, calls onResume.
    - Completion state: when displaySeconds === 0, show checkmark icon and pulse/glow effect (simple opacity animation with reanimated)

    **4. ingredients-sheet.tsx** — `IngredientsSheet` component
    Props: `{ ingredientGroups: IngredientGroup[], checkedIndices: number[], onToggleCheck: (flatIndex: number) => void, visible: boolean, onClose: () => void }`
    - Renders as a Modal (transparent background overlay) when `visible=true`
    - Content: white container sliding up from bottom (80% screen height), rounded top corners (20px)
    - Header: "Malzemeler" title + close (X) button
    - Scrollable list of ALL ingredients from ALL groups, flattened with a flat index
    - Each ingredient row: checkbox (checked = grey text + strikethrough), ingredient text "{amount} {unit} {name}", and "Degistir" button on the right
    - Checkbox tap calls `onToggleCheck(flatIndex)` — parent manages state
    - "Degistir" button: on tap, show a small tooltip/alert "Yakin zamanda! Bu ozellik cok yakinda geliyor." (coming soon). Use a simple Alert.alert or a temporary tooltip text below the button.
    - Group labels shown as section headers if group.label is not null

    **5. timer-indicator.tsx** — `TimerIndicator` component
    Props: `{ timerStepIndex: number, currentStepIndex: number, displaySeconds: number, onPress: () => void }`
    - Only renders when `timerStepIndex !== currentStepIndex` and `displaySeconds > 0`
    - Small floating pill (position: absolute, bottom: 100, right: 16) with semi-transparent background
    - Shows: arrow icon (left if timerStepIndex < currentStepIndex, right if >) + MM:SS remaining
    - Tapping calls onPress (parent navigates pager to timerStepIndex)

    **6. completion-screen.tsx** — `CompletionScreen` component
    Props: `{ recipeName: string, totalCookingTime: number, onBackToRecipes: () => void }`
    - Full screen centered layout
    - Large celebration icon (party-popper or food icon from MaterialCommunityIcons)
    - "Afiyet olsun!" text, large, bold, brand orange
    - Recipe name below in gray
    - Total cooking time formatted as "X dakika" (totalCookingTime in minutes)
    - "Tariflere Don" button: brand orange, navigates back to feed

    **7. resume-banner.tsx** — `ResumeBanner` component
    Props: `{ recipeName: string, currentStep: number, totalSteps: number, onResume: () => void, onDismiss: () => void }`
    - Horizontal banner with warm background (#FEF3EC)
    - Text: "Yarim kalan tarifin var - devam et?"
    - Recipe name + step progress: "{recipeName} ({currentStep+1}/{totalSteps})"
    - Two buttons: "Devam Et" (brand orange, calls onResume) and "Kapat" (gray text, calls onDismiss)
    - 12px borderRadius, 16px padding, subtle shadow

    **After building all components, run tests again — they MUST pass (GREEN).**
  </action>
  <verify>
    <automated>cd TheCook && npx jest __tests__/step-content.test.ts -x && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>All 7 cooking components created under components/cooking/, StepContent render tests pass confirming instruction, why (tap-to-reveal), looksLikeWhenDone, commonMistake, and recovery fields are rendered. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
```bash
cd TheCook && npx jest __tests__/step-content.test.ts -x && npx tsc --noEmit
```
No type errors. StepContent render tests green. All components export correctly. Recipe preview screen renders without crashes.
</verification>

<success_criteria>
- app/recipe/[id].tsx is the cooking preview screen with Steps Preview boxes and "Start Cooking" button
- 7 components exist in components/cooking/ with correct exports
- StepContent shows all COOK-02 (instruction, why, looks-like) and COOK-03 (common mistake, recovery) fields
- __tests__/step-content.test.ts passes with 7 assertions covering all StepContent content sections
- CircularTimer uses react-native-svg + reanimated for smooth ring animation
- IngredientsSheet has checkboxes and "Degistir" coming-soon placeholders
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-guided-cooking-mode/05-02-SUMMARY.md`
</output>
