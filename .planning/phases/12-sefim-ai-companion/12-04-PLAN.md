---
phase: 12-sefim-ai-companion
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - TheCook/__tests__/step-content.test.ts
  - TheCook/src/hooks/__tests__/useSefim.test.ts
autonomous: true
gap_closure: true
requirements: [COOKX-04, COOKX-05]

must_haves:
  truths:
    - "TypeScript compiles cleanly with zero errors across all test files"
    - "All existing Jest tests continue to pass"
  artifacts:
    - path: "TheCook/__tests__/step-content.test.ts"
      provides: "RecipeStep mock with sefimQA field"
      contains: "sefimQA"
    - path: "TheCook/src/hooks/__tests__/useSefim.test.ts"
      provides: "Correct Turkish category enum and typed renderHook"
      contains: "kahvaltı"
  key_links: []
---

<objective>
Fix 5 TypeScript compilation errors in Phase 12 test files identified by verification.

Purpose: The Phase 12 verification report found TSC exits non-zero due to 3 test file defects (5 individual errors). All are one-liner fixes in test mocks/generics. Source code is correct -- only test files need updating.

Output: Clean `npx tsc --noEmit` and all Jest tests passing.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sefim-ai-companion/12-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TypeScript errors in step-content.test.ts and useSefim.test.ts</name>
  <files>TheCook/__tests__/step-content.test.ts, TheCook/src/hooks/__tests__/useSefim.test.ts</files>
  <action>
Apply these 3 fixes:

1. In `TheCook/__tests__/step-content.test.ts` line 37-48: Add `sefimQA: []` to the `mockStep` object literal (after the `warning: null` line). The `RecipeStep` type now requires this field since Phase 12 added it to StepSchema.

2. In `TheCook/src/hooks/__tests__/useSefim.test.ts` line 98: Change `category: "kahvalti"` to `category: "kahvaltı"` (Turkish dotless i). The CategoryEnum requires the Turkish spelling with ı.

3. In `TheCook/src/hooks/__tests__/useSefim.test.ts` lines 262-264: Add generic type parameters to the `renderHook` call in the "linger resets to false on step index change" test. Change:
   ```
   const { result, rerender } = renderHook(
     ({ stepIndex }) => useSefim(sampleRecipe, stepIndex, "beginner", {}, 2),
     { initialProps: { stepIndex: 0 } }
   );
   ```
   to:
   ```
   const { result, rerender } = renderHook<{ stepIndex: number }, ReturnType<typeof useSefim>>(
     ({ stepIndex }) => useSefim(sampleRecipe, stepIndex, "beginner", {}, 2),
     { initialProps: { stepIndex: 0 } }
   );
   ```
   This gives TypeScript the generic context to infer the `rerender` parameter type and `result.current` type.
  </action>
  <verify>
    <automated>cd TheCook && npx tsc --noEmit 2>&1 | head -20 && npx jest --testPathPattern="step-content|useSefim" --no-coverage 2>&1 | tail -10</automated>
  </verify>
  <done>TSC exits 0 with no errors. All Jest tests in step-content.test.ts and useSefim.test.ts pass.</done>
</task>

</tasks>

<verification>
1. `cd TheCook && npx tsc --noEmit` exits with code 0
2. `cd TheCook && npx jest --no-coverage` — full suite passes
</verification>

<success_criteria>
- TypeScript compiles cleanly (zero errors)
- All existing tests pass (no regressions)
- Phase 12 VERIFICATION gap "TSC exits non-zero" is resolved
</success_criteria>

<output>
After completion, create `.planning/phases/12-sefim-ai-companion/12-04-SUMMARY.md`
</output>
