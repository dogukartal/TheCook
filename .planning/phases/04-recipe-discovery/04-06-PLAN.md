---
phase: 04-recipe-discovery
plan: "06"
type: execute
wave: 4
depends_on:
  - "04-04"
  - "04-05"
files_modified:
  - TheCook/app/settings.tsx
autonomous: false
requirements:
  - DISC-01
  - DISC-02
  - DISC-03
  - DISC-04
  - DISC-05

must_haves:
  truths:
    - "User can browse Feed tab and see 2-column recipe grid with skill ordering"
    - "Allergen-conflicting recipes never appear on any screen"
    - "User can type an ingredient, see autocomplete, pin a chip, and see filtered results"
    - "User can bookmark a recipe from any card and find it in My Kitchen saved section"
    - "Tab bar shows Keşfet, Ara, Mutfağım labels on physical device/simulator"
  artifacts:
    - path: "TheCook/app/settings.tsx"
      provides: "Settings sub-screen (moved from old settings.tsx content) accessible from My Kitchen gear icon"
      contains: "saveProfile"
  key_links:
    - from: "TheCook/app/(tabs)/my-kitchen.tsx"
      to: "TheCook/app/settings.tsx"
      via: "router.push('/settings') from gear icon tap"
      pattern: "router.push"
---

<objective>
Create the Settings sub-screen (preserving the profile editing functionality from the old settings tab), then do a full end-to-end human verification of all five DISC requirements across all surfaces.

Purpose: The Phase 4 human verification checkpoint. Every DISC requirement is confirmed working by a human on the actual device/simulator. Settings sub-screen is the final missing piece — the gear icon in My Kitchen needs a real destination.
Output: app/settings.tsx (profile editing sub-screen) + human sign-off that all 5 DISC requirements are met.
</objective>

<execution_context>
@/Users/sado/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sado/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-recipe-discovery/04-CONTEXT.md
@.planning/phases/04-recipe-discovery/04-04-SUMMARY.md
@.planning/phases/04-recipe-discovery/04-05-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts for executor. -->

From TheCook/src/auth/useSession.tsx (existing):
```typescript
export function useSession(): { session: Session | null; signOut: () => Promise<void>; }
```

From TheCook/src/db/profile.ts (existing):
```typescript
export function useProfileDb(): {
  getProfile(): Promise<Profile>;
  saveProfile(profile: Partial<Profile>): Promise<void>;
  addBookmark(recipeId: string, userId: string | null): Promise<void>;
  removeBookmark(recipeId: string): Promise<void>;
  getBookmarks(): Promise<Bookmark[]>;
}
```

Note: The old TheCook/app/(tabs)/settings.tsx has been deleted. Its content (allergen chips, skill chips, equipment grid, account card, all labels/icons) must be reproduced in TheCook/app/settings.tsx. The phase 02 SUMMARY and my-kitchen.tsx SUMMARY document these patterns in full.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Settings sub-screen</name>
  <files>TheCook/app/settings.tsx</files>
  <action>
Create TheCook/app/settings.tsx as the profile editing sub-screen. This is NOT a tab — it is a regular full-screen route pushed via router.push('/settings') from the My Kitchen gear icon.

The screen replicates the full content of the old settings.tsx tab (now deleted in Plan 04):
- Screen header row: back button (router.back()) + "Ayarlar" title
- Account section: signed-in card with provider label + sign-out button; not-signed-in card with "Create an account" link
- Allergen chips section with immediate-save on toggle
- Skill level chips section with immediate-save
- Equipment grid section with immediate-save

Copy all label maps, icon maps, toggle handlers, and persistProfileChange logic exactly from the Phase 02 SUMMARY or from the my-kitchen.tsx reference. The immediate-save UX (no Save button) is an established project pattern from Phase 2 — maintain it.

This is a straightforward copy-and-refactor. The only changes from the old settings.tsx are:
1. Added back button in header (router.back())
2. Title changed to "Ayarlar" (not "Settings")
3. Screen is reached via router.push, not a tab tap

After creating the file, verify the gear icon in My Kitchen navigates here correctly.
  </action>
  <verify>
    <automated>cd TheCook && npx tsc --noEmit 2>&1 | grep "settings" | head -10</automated>
  </verify>
  <done>TheCook/app/settings.tsx compiles. router.push('/settings') from My Kitchen gear icon navigates to this screen. Profile editing (allergens, skill, equipment) works with immediate-save.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of all DISC requirements</name>
  <action>
Run the app on simulator: `cd TheCook && npx expo start`

Verify each requirement by following these steps:

**DISC-02 — Feed + skill ordering:**
1. Open Feed tab. Confirm bottom tab bar shows "Keşfet", "Ara", "Mutfağım"
2. Confirm Feed shows 2-column recipe grid with gradient card backgrounds per category
3. Tap "Sizin İçin" tab — confirm recipes reorder (beginner recipes first if skill level set)
4. Tap category chips (Kahvaltı, Çorba, etc.) — confirm grid filters to that category
5. Tap ▼ to open advanced filter — confirm cuisine/cook time/skill panels appear

**DISC-01 — Ingredient search:**
6. Open Search tab. Confirm idle state shows recently viewed row-cards (empty on first use)
7. Type "soğan" — confirm autocomplete dropdown shows ingredient suggestions
8. Tap an ingredient — confirm it becomes a pinned chip below search bar
9. Add a second ingredient — confirm results show recipes containing BOTH ingredients
10. Tap × on a chip — confirm it's removed and results update

**DISC-04 — Bookmarks:**
11. Tap the ♡ icon on any recipe card — confirm it fills (bookmarked state)
12. Open My Kitchen tab — confirm the bookmarked recipe appears in "Kaydedilen Tarifler" section
13. Tap ♡ again on the same card — confirm it unfills and disappears from My Kitchen

**DISC-05 — Allergen filtering:**
14. Go to My Kitchen → tap gear icon → Ayarlar screen opens
15. Set an allergen (e.g. Dairy)
16. Go back to Feed — confirm no recipes containing dairy allergen appear
17. Go to Search tab — type an ingredient — confirm dairy recipes excluded from results

**Recipe detail:**
18. Tap any recipe card — confirm detail screen opens with title, gradient hero, ingredients list, steps list
19. Confirm back button returns to previous screen

**Offline:**
20. Enable Airplane mode — confirm Feed and Search still work with no network
  </action>
  <verify>
    <automated>cd TheCook && npx jest --no-coverage</automated>
  </verify>
  <done>Human confirms all 5 DISC requirements work on device/simulator. Full test suite passes. Phase 4 complete.</done>
</task>

</tasks>

<verification>
Final automated check after human approval:
```bash
cd TheCook && npx jest --no-coverage
```
Full test suite green before phase complete.
</verification>

<success_criteria>
- app/settings.tsx exists and is reachable from My Kitchen gear icon
- Human approves all 5 DISC verification steps
- Allergen filtering confirmed working on Feed + Search surfaces (DISC-05)
- Bookmarks persist across app restart (DISC-04)
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-recipe-discovery/04-06-SUMMARY.md`
</output>
