// Wave 0 scaffold — assertions filled in by Plan 02 (schema implementation)
// Import will fail until src/types/recipe.ts exists — that is expected at this stage
import { RecipeSchema } from "../src/types/recipe";

describe("RecipeSchema", () => {
  it.todo("validates complete recipe");
  it.todo("rejects incomplete step");
  it.todo("rejects invalid unit");
  it.todo("rejects invalid allergen");
  it.todo("rejects invalid equipment");
});
